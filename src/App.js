import * as React from 'react';

import Bmi from './containers/Bmi/Bmi';

class App extends React.Component<{}> {
  render() {
    return (
      <main>
        <Bmi />
      </main>
    );
  }
}

export default App;
